<b><%= @user_name %></b> has shared <a href=<%= Rails.env=="development" ? "http://localhost:3000/communities/#{@community_id}" : "https://www.meetlinkshare.com/communities/#{@community_id}" %>>
<% unless @files_length<=0%> <%= @files_length%> files <% end%><%= (@files_length >0) && (@folders_length >0) ? " and " : ''%>
<% unless @folders_length<0%><%= @folders_length%> folders <% end%> </a>
in MeetLinkshare virtual team called <b><%= @community_name%></b><br/><br/>
<a href=<%= Rails.env=="development" ? "http://localhost:3000/communities/#{@community_id}" : "https://www.meetlinkshare.com/communities/#{@community_id}" %>>If you do not want to receive notifications for this team, please turn them off here</a>